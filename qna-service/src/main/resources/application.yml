server:
  port: 8033

spring:
  application:
    name: qna-service

# Swagger Setting
openapi:
  service:
    title: qna-service doc
    version: v1
    url: https://api.buysellgo.shop/${spring.application.name}
springdoc:
  api-docs:
    path: /qna-service/v3/api-docs
  swagger-ui:
    path: /qna-service/swagger-ui.html

# log level setting
logging:
  level:
    logging:
      level:
        root: info
        org.hibernate.orm.jdbc.bind: info
        org.hibernate.SQL: info
        springframework.cloud.config: info
        com.buysellgo: info